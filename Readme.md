# Nginx redirects to docker container

Simple example for container Nginx redirects to another container (API).

#### Run

```
docker-compose up -d
```

#### Open in browser

```
http://localhost/api
```
